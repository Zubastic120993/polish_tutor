name: 🚀 Deployment Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout repo
        uses: actions/checkout@v4

      - name: 🧹 Full clean of caches and compiled files
        run: |
          echo "Cleaning all caches and compiled Python files to avoid stale mypy data..."
          rm -rf ~/.cache/pip ~/.cache/mypy_cache .mypy_cache || true
          find . -type d -name "__pycache__" -exec rm -rf {} + || true
          find . -type f -name "*.pyc" -delete || true
          echo "✅ Workspace fully cleaned."

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: ⚙️ Set up Node.js (needed for Playwright browsers)
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Ensure Playwright, pytest, and Mypy are available
          pip install playwright pytest-playwright mypy

      - name: 🎭 Install Playwright browsers
        uses: microsoft/playwright-github-action@v1
        with:
          with-deps: true

      - name: 🔍 List installed Playwright browsers
        run: python - <<'PY'
import pathlib
base = pathlib.Path.home() / ".cache" / "ms-playwright"
print(f"Listing contents of {base}")
if base.exists():
    for path in base.rglob("*"):
        if path.is_file():
            print(path.relative_to(base))
else:
    print("Playwright cache directory not found")
PY

      - name: 🧠 Run Mypy type checks (non-blocking)
        run: |
          echo "Running Mypy type checks..."
          mypy backend/tts/tasks.py || true
          mypy . || true

      - name: 🧪 Run full test suite
        run: pytest -v

      - name: 🐳 Build Docker image
        run: docker build -t polish_tutor_app .

      - name: 🚀 Push Docker image to GitHub Container Registry
        if: github.ref == 'refs/heads/main'
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker tag polish_tutor_app ghcr.io/${{ github.repository_owner }}/polish_tutor_app:latest
          docker push ghcr.io/${{ github.repository_owner }}/polish_tutor_app:latest