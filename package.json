{
  "name": "pol_app",
  "version": "1.0.0",
  "description": "An AI-assisted conversational tutor for learning spoken Polish (A0 → A1 level).",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "dev": "tailwindcss -i ./frontend/static/css/main.css -o ./frontend/static/css/output.css --watch",
    "build": "tailwindcss -i ./frontend/static/css/main.css -o ./frontend/static/css/output.css --minify",
    "build:css": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.10",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.14"
  }
}
